package textFile;

public class VideoFile extends File{

    VideoFile(String fileName, int size, String fileExtension, String content) {
        super(fileName, size, fileExtension, content);
    }
}
